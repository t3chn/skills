# Claude Code

Claude Code-specific assets live under `claude/`.

Planned structure (mirrors Claude Code plugin conventions):

```
claude/plugins/<plugin-name>/
├── .claude-plugin/plugin.json
├── .mcp.json              # optional
├── commands/              # optional
├── agents/                # optional
└── skills/                # optional
```

